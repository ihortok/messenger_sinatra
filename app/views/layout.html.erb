<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Messenger</title>
</head>
<body>

  <%= erb :'header.html' %>

  <% if logged_in? %>
    <%= erb :'aside.html' %>
  <% end %>

  <% unless locals.empty? %>
    <%= message %>
  <% end %>

  <%= yield %>

</body>
</html>
